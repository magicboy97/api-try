import { FiterUserPipe } from './fiter-user.pipe';

describe('FiterUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterUserPipe();
    expect(pipe).toBeTruthy();
  });
});
